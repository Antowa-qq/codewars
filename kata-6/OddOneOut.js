function oddOneOut(str) {

    // console.log([...str].indexOf('l',3));
    // return [...str].filter((item, index, arr) => item != arr.indexOf(item, index + 1));
    // let result = [...str].map((item, index, arr) => {
    //     if (item === arr[arr.indexOf(item, index + 1)]) {
    //         arr.slice(index, 1);
    //         arr.slice(arr.indexOf(item, index + 1));
    //     }
    //     return item;
    // });

    return result;


}
console.log(oddOneOut('Hello world'));
